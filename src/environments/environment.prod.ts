/**
 * Production environment. Use via angular.json fileReplacements for production build.
 * Never put real API keys or secrets here.
 */
export const environment = {
  apiUrl: '', // Set to your backend URL, e.g. 'https://api.apartel.app'
  demoLoginHint: '',
} as const;
